@model web1.Models.PagedResult<web1.Models.Book>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("BookTitle")
        </th>
        <th>
            @Html.DisplayName("BookContent")
        </th>
        <th>
            @Html.DisplayName("BookMetaDescription")
        </th>
        <th>
            @Html.DisplayName("BookMetaKeywords")
        </th>
        <th>
            @Html.DisplayName("BookMetaAuthor")
        </th>
        <th>
            @Html.DisplayName("BookCreatedTime")
        </th>
        <th>
            @Html.DisplayName("BookUpdatedTime")
        </th>
        <th></th>
    </tr>
    @*@{ var i = 1; }
        @foreach (var contact in Model.Results)
        {
            var rowNo = (Model.CurrentPage - 1) * Model.PageSize + i;
            i++;
        }*@

    @*@foreach (var item in Model)
        {*@
    @foreach (var item in Model.Results)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BookTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookContent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookMetaDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookMetaKeywords)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookMetaAuthor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookCreatedTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookUpdatedTime)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.BookID }) |
                @Html.ActionLink("Details", "Details", new { id = item.BookID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.BookID }) |
                @Html.ActionLink("New Section","Create","Section",new {id=item.BookID},null)
            </td>
        </tr>
    }

</table>
@for (var p = 1; p <= Model.PageCount; p++)
{
    <a href="@Url.Action("Index", new { page = p })" class="btn btn-default">@p</a>
}